# cardmem — secret-bearing files, never commit
.mcp.json
.env
.env.*
!.env.example
.claude/settings.local.json

# build output + generated files
dist
node_modules
src/version.ts

# local backup artifacts
*.bak
